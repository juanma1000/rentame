server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router (BrowserRouter) client-side routing: unknown paths fall
    # back to index.html so the app's own router can resolve them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
